# 이 프로젝트의 API 문서화 규칙

FastAPI 프로젝트의 문서 관련 코드를 작성·수정할 때 아래 규칙을 따른다.

이 문서의 목적은 AI에게 API 문서화 작업을 요청할 때 코드 로직을 망가뜨리지 않고, 실제 코드에 근거한 문서만 만들게 하는 것이다.

---

## 1. 절대 지킬 것

- 함수 본문, 즉 실제 로직은 절대 수정하지 않는다.
- 문서화와 직접 관련된 부분만 수정한다.
- 허용 범위는 `tags`, `summary`, `responses`, 독스트링, 모델 필드의 `description`, `examples`, `json_schema_extra` 추가로 제한한다.
- 엔드포인트 선언 순서, 함수 순서, 기존 데코레이터 인자, 기존 제약 조건은 그대로 유지한다.
- 코드에 실제로 존재하는 동작만 문서에 적는다.
- 코드에 없는 기능, 오류, 응답, 파라미터는 추측해서 지어내지 않는다.
- 애매하거나 확인하지 못한 내용은 임의로 수정하지 않고 `확인 필요`로 보고한다.
- 작업 후에는 변경 내용, 코드 근거, `확인 필요` 항목을 함께 제시한다.

---

## 2. 작업 전 확인 원칙

문서화 작업을 시작하기 전에 엔드포인트 경로와 HTTP 메서드, 기존 데코레이터 인자, 함수 시그니처, 실제 파라미터, 실제 `raise HTTPException`, 실제 `response_model`, Pydantic 모델 필드와 기본값, `Field`, `Query`, `Path`, `Body` 설정을 확인한다.

확인하지 못한 내용은 문서화하지 않는다.

---

## 3. 태그 규칙

엔드포인트 데코레이터의 `tags`에는 아래 4개만 사용하고, 새 태그를 만들지 않는다.

| 태그 | 대상 |
|---|---|
| 시스템 | `/`, `/health`, `/info` |
| 도서 | `/books` 관련 전부. 단, `/books/external`, `/books/external/multi` 제외 |
| 외부 연동 | `/weather`, `/weather/raw`, `/books/external`, `/books/external/multi` |
| 학습용 | `/slow-async`, `/slow-block` |

태그 표에 없는 신규 엔드포인트가 기존 4개 태그 중 어디에도 명확히 속하지 않으면 `tags`를 추가하지 않고 `확인 필요`로 보고한다.

---

## 4. summary 규칙

`summary`는 Swagger UI에서 엔드포인트를 한눈에 이해하기 위한 짧은 제목이다.

- 한국어 명사형 한 줄로 작성하고, 문장형이나 동사형 표현은 사용하지 않는다.
- 너무 길게 쓰지 않고, 실제 동작과 다르게 쓰지 않는다.
- 실제 동작이 명확하지 않으면 `summary`를 추가하지 않고 `확인 필요`로 보고한다.

---

## 5. 독스트링 규칙

독스트링은 함수 정의 바로 다음 줄에 삼중따옴표로 작성한다.

- 설명 문장은 한국어로 작성하되, 코드 식별자, 필드명, 상태 코드, 클래스명은 원문 그대로 사용한다.
- `Args`, `Returns`, `Raises` 같은 영어 스타일은 사용하지 않는다.
- 파라미터가 있으면 `- **이름**: 설명` 형식으로 나열한다.
- 함수 안에 `raise HTTPException`이 있는 경우에만 오류 상태 코드를 언급한다.
- 명시적 `raise HTTPException`이 없으면 `코드에 명시된 HTTPException은 없다.`라고 쓴다.
- 코드에 없는 반환값, 처리 과정, 동작은 설명하지 않는다.
- 독스트링은 실제 코드로 확인 가능한 내용만 작성한다.
- 일부 항목만 확인 가능한 경우에는 확인 가능한 항목만 작성하고, 확인할 수 없는 항목은 `확인 필요`로 보고한다.

---

## 6. responses 규칙

- 코드에 `raise HTTPException(status_code=N, ...)`이 실제로 있는 상태 코드만 추가한다.
- `description`은 `HTTPException(detail="...")`의 문자열을 그대로 사용한다.
- `detail` 문자열에 마침표를 추가하거나 문장을 수정하지 않는다.
- `detail`이 문자열 리터럴이 아니면 임의로 문장을 만들지 않고 `확인 필요`로 보고한다.
- 같은 엔드포인트 안에서 동일한 상태 코드가 여러 번 `raise`되고 `detail`이 서로 다르면 해당 상태 코드는 `responses`에 추가하지 않는다.
- 동일 상태 코드의 서로 다른 `detail` 값은 모두 `확인 필요` 보고 표에 나열한다.
- `200`, `422`는 FastAPI가 자동 생성하므로 추가하지 않는다.
- 상태 코드는 문자열이 아니라 정수로 쓴다.
- 실제로 `raise` 하지 않는 상태 코드는 `responses`에 추가하지 않는다.

---

## 7. 모델 필드 문서화 규칙

- 이 프로젝트는 Pydantic v2 기준으로 작성한다.
- Pydantic 모델 필드에는 필요한 경우 `description`과 `examples`를 추가한다.
- `description`은 한국어 명사형 짧은 구로 작성한다.
- `examples`는 실제로 있을 법한 한국어 값으로 작성하고, 실제 자료형과 일치해야 한다.
- `"string"`, `"abc"`, `"test"` 같은 무의미한 값은 사용하지 않는다.
- 기존 제약 조건은 그대로 유지한다.
- 기본값이 있는 선택 필드를 `Field`로 감쌀 때는 `default=`를 반드시 명시한다.
- `field_validator` 함수는 건드리지 않는다.
- 필드 의미나 예시 자료형을 확정할 수 없으면 수정하지 않고 `확인 필요`로 보고한다.

---

## 8. 전체 예시 규칙

- `BookCreate`처럼 요청 본문으로 자주 쓰이는 모델에는 `model_config`의 `json_schema_extra`로 전체 예시를 추가할 수 있다.
- Pydantic v2 기준으로 `schema_extra`가 아니라 `json_schema_extra`를 사용한다.
- 실제 프로젝트에서 사용할 수 있는 자연스러운 값으로 작성한다.
- `"string"`, `"test"`, `"abc"` 같은 기본 예시는 사용하지 않는다.
- 필드 이름은 실제 모델에 존재하는 것만 사용한다.
- 선택 필드는 생략 가능한 경우와 포함하는 경우를 상황에 맞게 정한다.
- 모델에 없는 필드를 예시에 넣지 않는다.
- 각 필드의 자료형과 일치하는 값을 사용한다.
- 모델 전체 예시를 안전하게 작성할 근거가 부족하면 추가하지 않고 `확인 필요`로 보고한다.

---

## 9. 작업 후 보고 규칙

AI가 문서화 작업을 마친 뒤에는 변경 내용과 근거를 함께 보고해야 한다.

기본 보고 형식:

| 엔드포인트 | 추가한 항목 | 근거 |
|---|---|---|
|  |  |  |

오류 응답 문서화 작업 후에는 아래 표를 함께 작성한다.

| 엔드포인트 | 코드에서 찾은 raise 위치 | 추가한 responses 항목 |
|---|---|---|
|  |  |  |

작업 요약은 아래 형식으로 보고한다.

| 항목 | 내용 |
|---|---|
| 수정한 파일 |  |
| 수정한 엔드포인트 수 |  |
| 수정한 모델 수 |  |
| 함수 본문 수정 여부 |  |

`확인 필요`로 보고된 항목은 이번 작업에서 코드에 반영하지 않고, 사람이 확인한 뒤 별도 작업으로 처리한다.

확인 필요 항목은 아래 형식으로 보고한다.

| 위치 | 확인 필요 사유 | 코드에서 확인한 근거 | 이번 작업 처리 |
|---|---|---|---|
|  |  |  | 수정하지 않음 |

---

## 10. 누락 점검 규칙

문서화 작업을 마친 뒤에는 코드를 고치지 말고 아래 항목만 표로 점검한다.

점검 항목: `tags`가 없는 엔드포인트, `summary`가 없는 엔드포인트, 독스트링이 없는 엔드포인트, `raise HTTPException`은 있는데 `responses`에 없는 상태 코드, `responses`에는 있는데 실제로 `raise` 하지 않는 상태 코드, `description`이 없는 모델 필드, `summary`와 실제 동작 일치 여부, `examples` 자료형과 실제 필드 타입 일치 여부, 선택 필드의 `default=` 유지 여부, 문서에 없는 오류를 새로 만들었는지 여부, `확인 필요`로 보고해야 하는데 임의로 수정한 항목이 있는지 여부.

특히 `responses`에는 있는데 실제 코드에서 발생하지 않는 오류는 거짓 문서이므로 반드시 점검한다.

점검 표는 아래 형식으로 작성한다.

| 항목 | 위치 | 문제 | 수정 필요 여부 |
|---|---|---|---|
|  |  |  |  |

---

## 11. 변경 최소화 원칙

수정 diff에는 문서화 작업과 직접 관련된 변경만 나타나야 한다.

허용되는 변경은 `tags`, `summary`, `responses`, 독스트링, 모델 필드의 `description`, `examples`, `json_schema_extra` 추가로 제한한다.

그 외 `response_model`, `status_code`, `dependencies`, 함수명, 파라미터명, 반환문, `import`, 변수명, 줄바꿈 스타일, 공백 스타일, 함수 순서, 엔드포인트 선언 순서 변경은 금지한다.

문서화와 무관한 리팩터링은 하지 않으며, `확인 필요`로 분류된 항목은 diff에 반영하지 않는다.

---

## 12. 문서의 근거 원칙

모든 문서 내용은 실제 코드에서 확인 가능한 근거가 있어야 한다.

문서화할 수 있는 항목은 함수 시그니처, 데코레이터, Pydantic 모델, `Field` 설정, `Query`, `Path`, `Body` 설정, `raise HTTPException`, 실제 반환 타입 또는 `response_model`으로 제한한다.

존재하지 않는 기능, 존재하지 않는 오류, 존재하지 않는 파라미터, 존재하지 않는 응답 형식, 미래에 구현할 예정인 기능은 추측해서 작성하지 않는다.

---

## 13. 가장 중요한 원칙

문서화는 코드를 예쁘게 꾸미는 작업이 아니라, 실제 코드의 동작을 정확히 설명하는 작업이다.

문서는 항상 코드보다 앞서가면 안 된다.

코드에 없는 내용은 문서에 쓰지 않는다.

실제로 발생하지 않는 오류를 문서에 적으면 거짓 문서가 된다.

AI에게 문서화를 맡길 때는 항상 근거를 함께 요구한다.